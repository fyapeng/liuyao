import type { Hexagram } from "../types/index.ts";

export function getSixRelatives(_lines: Hexagram["lines"]) {
  return {
    status: "placeholder",
    relatives: [],
    message: "六亲计算接口已预留，后续将基于纳甲五行与用神规则生成。"
  };
}
